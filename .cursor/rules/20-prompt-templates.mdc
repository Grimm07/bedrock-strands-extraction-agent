---
description: Jinja template invariants for src/bedrock_strands_agent/templates/. Auto-attaches when editing any *.j2 — these templates are the highest-blast-radius surface in the codebase; a regression silently degrades extraction across every schema.
globs: src/bedrock_strands_agent/templates/*.j2
alwaysApply: false
---

# Prompt template invariants

The Jinja templates in `src/bedrock_strands_agent/templates/`
(`system.j2`, `extract.j2`, `retry.j2`, `extract_image.j2`,
`verify_grounding.j2`) drive every Bedrock call. A silent regression
here degrades extraction across every schema. Before suggesting a
change, verify each invariant below.

## Invariants

1. **JSON shape contract preserved.** Top-level `{"fields": [...]}`
   with each entry shaped `{name, value, confidence,
   source_excerpt}`.
   Verify: `system.j2` and `extract.j2` both spell out this shape;
   `retry.j2` restates it (the model may have lost system context
   across the retry hop).

2. **No-fences instruction present in `system.j2` and `extract.j2`.**
   Each template must explicitly tell the model not to wrap the JSON
   in Markdown code fences. The parser tolerates fences via
   `_FENCE_RE`, but un-fenced is preferred — don't rely on the safety
   net.

3. **StrictUndefined safety.** Every `{{ ... }}` and `{% ... %}`
   variable reference must be one `PromptRenderer` actually passes
   in. Cross-check against `agent/prompts.py`. Allowed names:
   - `system.j2`: `service_name`, `service_env`, `model_id`
   - `extract.j2`: `schema` (with `.name`, `.version`, `.description`,
     `.fields[*].name`, `.type.value`, `.required`, `.description`,
     `.pattern`, `.examples`), `document_text`
   - `retry.j2`: `schema.name`, `document_text`, `errors`
   Anything else (e.g. `field.label`) raises `UndefinedError` at
   runtime.

4. **`extract.j2` injects every relevant field attribute.** The
   `{% for field in schema.fields %}` loop must emit at minimum
   `name`, `type` (or `type.value`), `required`, `description`, plus
   conditional `pattern` and `examples`. Dropping any silently
   reduces extraction quality — the model loses hints.

5. **`retry.j2` includes `errors` and `document_text`.** The errors
   list must be iterated and rendered; the document must be
   re-included verbatim. Missing errors makes retry a re-roll;
   missing document leaves the model with no source.

6. **No PII templating mistakes.** `document_text` belongs in the
   prompt *body*, treated as data — never interpolated into a
   system-prompt field the model would treat as instructions, and
   never gated by a `{% if %}` that could route it to the wrong
   context. `document_text` appears only inside `extract.j2`,
   `retry.j2`, and `verify_grounding.j2` (never `system.j2`),
   wrapped in clear delimiter markers (`<document>...</document>`),
   outside any conditional that changes its meaning.

If every invariant passes, say so and stop. If a change violates any
invariant, report which one and why, propose the minimal fix, and
stop.
